 query search($searchTerm: String) {

   search(searchTerm: $searchTerm) {
     stations {
       name
       location {
         latitude
         longitude
       }
       picture {
         url
       }
     }
   }
  }